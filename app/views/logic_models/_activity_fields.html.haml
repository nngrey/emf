.nested-fields
  %h3 Add activities
  = f.inputs do
    = f.input :description, label: "Activity description", :input_html => { :rows => 2, :id => 'activity'}
    %p= link_to_remove_association "<button, class='btn btn-warning'>Remove this activity</button>".html_safe, f