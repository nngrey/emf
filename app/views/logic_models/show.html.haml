.container
  %h4= "Logic model for #{ @logic_model.program.name }"
  .table-responsive
    %table.table.table-bordered
      %thead
        %tr
          %th Inputs
          %th Activities
          %th Outputs
          %th Outcomes
          %th Impacts
      %tbody
        %tr
          %td
            %ul
              - @logic_model.logic_model_inputs.each do |input|
                %ol= input.description
          %td
            %ul
              - @logic_model.activities.each do |activity|
                %ol= activity.description
          %td
            %ul
              - @logic_model.outputs.each do |output|
                %ol= output.description
          %td
            %ul
              - @logic_model.outcomes.each do |outcome|
                %ol= outcome.description
          %td
            %ul
              - @logic_model.impacts.each do |impact|
                %ol= impact.description

