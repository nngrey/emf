.nested-fields
  %h3 Add outcomes
  = f.inputs do
    = f.input :description, label: "Outcome description", :input_html => { :rows => 2, :id => 'outcome'}
    %p= link_to_remove_association "<button, class='btn btn-warning'>Remove this outcome</button>".html_safe, f