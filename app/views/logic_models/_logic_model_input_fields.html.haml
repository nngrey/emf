.nested-fields
  %h3 Add inputs
  = f.inputs do
    = f.input :description, label: "Input description", :input_html => { :rows => 3, :id => 'input'}
    %p= link_to_remove_association "<button, class='btn btn-warning'>Remove this input</button>".html_safe, f