!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Emf
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %nav.navbar.navbar-default
      .container-fluid{:style => "padding-left: 40px"}
        .navbar-header
          %button.navbar-toggle.collapsed{"aria-expanded" => "false", "data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          - if Organization.count == 0
            %a.navbar-brand{:href => new_organization_path} Set up
          - else
            %a.navbar-brand{:href => organization_path(Organization.first)}
              = Organization.first.name.titleize
        #bs-example-navbar-collapse-1.collapse.navbar-collapse
          %ul.nav.navbar-nav
            - if EvaluativeQuestion.count > 0
              - Framework.all.each do |framework|
                %li
                  %a{:href => dashboard_framework_path(framework)}
                    = framework.name.titleize

            - if Organization.count > 0
              %li
                %a{:href => frameworks_path} Frameworks
            - if SurveyTemplate.any?
              %li
                %a{:href => survey_templates_path} Surveys
    = yield
